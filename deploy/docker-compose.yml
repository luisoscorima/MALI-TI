version: "3.8"

services:
  nginx-static:
    image: nginx:latest
    container_name: web-projects-mali
    ports:
      - "8083:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
